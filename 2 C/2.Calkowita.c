#include <stdio.h>
int main(void)
{
    int x;
    // deklaracja zmiennej całkowitej
    printf("Podaj wartość x: ");
    scanf("%d", &x);
    // czyta dane z stdin - liczba całkowita w systemie dziesiętnym
    // &x to adres zmiennej 
    return 0;
}